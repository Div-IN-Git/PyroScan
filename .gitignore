# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
.env

# Large model files — upload separately or use Git LFS
# Comment out the line below if your model is under 100MB
# models/*.pkl

# Keep our trained model (311KB — under GitHub's 100MB limit)
!models/wildfire_model.pkl

# Remove the broken models that crash on load
models/continent_model.pkl
models/global_model.pkl
models/local_model.pkl
models/fire_risk_model.pkl

# Vercel
.vercel/

# OS
.DS_Store
Thumbs.db
